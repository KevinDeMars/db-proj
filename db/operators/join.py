from typing import Generator, List

from db.page import Page
from db.relation import Relation

def join(r1: Relation, r2: Relation, attrs: List[str]) -> Relation:
    pass


def _pages(r1, r2, attrs: List[str]) -> Generator:
    pass
